[project]
name = "tradechain"
version = "0.2.0"
description = "AI investment research agent system foundation"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
